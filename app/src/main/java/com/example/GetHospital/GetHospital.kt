package com.example.GetHospital

import android.os.Bundle
import androidx.fragment.app.Fragment
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import com.example.work.R


class GetHospital : Fragment() {

    private lateinit var viewModel: gethospital_ViewModel
    private lateinit var viewModelFactory: gethospital_ViewModelFactory

  /*  override fun onCreateView(
        inflater: LayoutInflater,
        container: ViewGroup?,
        savedInstanceState: Bundle?
    ): View? {

        // Inflate view and obtain an instance of the binding class.
        /*  val binding: ScoreFragmentBinding

        viewModelFactory = gethospital_ViewModelFactory(ScoreFragmentArgs.fromBundle(arguments!!).score)
        viewModel = ViewModelProviders.of(this, viewModelFactory)
            .get(gethospital_ViewModel::class.java)



        return binding.root */
    }*/
}